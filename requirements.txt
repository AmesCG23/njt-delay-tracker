# NJT Delay Tracker — Python dependencies
# Install with: pip install -r requirements.txt

# Bluesky (reading alert accounts + posting results)
atproto==0.0.55

# Claude API (interpreting alert text)
anthropic==0.40.0

# Google Sheets (logging events)
gspread==6.1.2
google-auth==2.29.0

# Timezone database — provides IANA data for zoneinfo on minimal Linux runners
tzdata==2025.2
